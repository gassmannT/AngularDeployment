import { browser, element, by } from 'protractor';

describe("QuickStart E2E Tests", () => {

    beforeEach(() => {
        browser.get("/");
    });

    it("should display title", () => {
        // Arrange
        // Act
        // Assert
    });

});