import { browser, by, element } from 'protractor/built';

describe("Session update", () => {
    beforeEach(() => {
        browser.get("/");
    });

    it("should show four sessions when we first load the app", () => {
        // Arrange
        // Act
        // Assert
    });

    // it("should show the first session detail page", () => { });

    // it("should update the first session firstname", () => { });

});